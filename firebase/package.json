{
  "name": "@wasat/firebase-rules-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Тесты Firestore/Storage Security Rules (ТЗ §4.3, §14)",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test --import tsx \"test/**/*.test.ts\"",
    "test:emulator": "firebase emulators:exec --only firestore --project demo-wasat \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
