# Верификация реализации против ТЗ v2.2

Документ фиксирует соответствие реализованного кода Техническому заданию
(«Android Multi-Store E-Commerce Platform», v2.2) и перечень оставшихся задач.
Дата сверки: 2026-06-13. Метод: построчная вычитка ТЗ (§1–§18) + три параллельных
аудита кода (покупатель / владелец / суперадмин+инфраструктура) с привязкой к файлам.

## Итог

Из **24 MVP-требований**: **~20 готовы (код-комплит)**, **3 частично**
(FR-B11, FR-A12, FR-S05), плюс «дыры под внешние ключи» внутри в остальном готовых
фич (живые платежи в FR-B05). FR-A08/A10 — Post-MVP по самому ТЗ.
Инфраструктура, безопасность (Firestore Rules + тесты, App Check, rate-limit, Zod),
CI/CD, локализация (ru/en), edge-to-edge и offline-first — реализованы.

Обозначения: ✅ готово · 🟡 частично · 🚫 заблокировано внешней зависимостью ·
⏸ Post-MVP (по ТЗ).

## Покупатель (§5)

| FR | Описание | Статус | Примечание |
|----|----------|--------|------------|
| FR-B01 | Открытие магазина по ссылке/QR | ✅ | deep link + QR + резолв slug + кэш последнего магазина |
| FR-B02 | Каталог | ✅ | Paging 3, фильтры, сортировки; поиск — серверный fallback (не Algolia) |
| FR-B03 | Карточка товара | ✅ | HorizontalPager + zoom, варианты, отзывы |
| FR-B04 | Корзина | ✅ | Room + Firestore-sync + слияние + офлайн |
| FR-B05 | Чекаут | ✅ (ядро) | транзакция + идемпотентность; живой Stripe PaymentSheet/Google Pay и email — отложены |
| FR-B06 | Трекинг/отмена заказа | ✅ | realtime listener, push, отмена NEW/CONFIRMED/PROCESSING |
| FR-B07 | Вишлист | ✅ | `customers/{uid}.wishlist`, только auth |
| FR-B08 | Отзывы и рейтинг | ✅ | серверная проверка покупки, запрет дубля, пересчёт рейтинга |
| FR-B09 | Запрос возврата | ✅ | выбор позиций + причина → `returns/{rid}` REQUESTED |
| FR-B10 | Уведомление о поступлении/цене | ✅ | подписка + серверный триггер + FCM |
| FR-B11 | Сохр. оплата + повторный заказ | 🟡 | адресная книга и «повторить заказ» есть; сохранённые карты (Stripe) — нет |
| FR-B12 | Рекомендации | ✅ | «недавно просмотренные» + похожие/популярное; явную кнопку «поделиться» подтвердить |

## Владелец магазина (§6)

| FR | Описание | Статус | Примечание |
|----|----------|--------|------------|
| FR-A01 | Настройка магазина | ✅ | вид, slug, тема, валюта, доставка, контакты, соцсети, часы, статус |
| FR-A02 | CRUD товаров | ✅ | мультизагрузка фото; миниатюры (Resize extension сконфигурирован, не задеплоен) |
| FR-A03 | Инвентарь | ✅ | атомарно + inventoryLog + CSV-импорт + порог |
| FR-A04 | Управление заказами | ✅ | единый enum, фильтры, авто-push, отмена, HTML-инвойс |
| FR-A05 | Аналитика | ✅ | выручка/чек/воронка/топ/новые-vs-повторные + CSV |
| FR-A06 | Промокоды | ✅ | типы, ограничения, scope, серверная проверка |
| FR-A07 | Push покупателям | ✅ | ручные/триггерные (брошенная корзина, статус, новый товар) |
| FR-A09 | Сотрудники | ✅ | приглашение, роли, staff-doc, auditLog |
| FR-A11 | Возвраты | ✅ (ядро) | очередь, статусы, ресток; живой Stripe Refund — отложен |
| FR-A12 | AI-ассист контента | ✅ (ядро) | генерация **и rewrite** описаний + Android-UI (env-gated); SEO-мета — Post-MVP по FR-A02 |
| FR-A08 | Блог/статьи | ⏸ | Post-MVP (схема зарезервирована) |
| FR-A10 | Программа лояльности | ⏸ | Post-MVP |

## Суперадминистратор (§7, веб-панель)

| FR | Описание | Статус | Примечание |
|----|----------|--------|------------|
| FR-S01 | Список всех магазинов | ✅ | поиск/фильтр/метрики |
| FR-S02 | Управление магазином | ✅ | блокировка с причиной + смена тарифа + read-only инспекция + auditLog (email-уведомление — отложено) |
| FR-S03 | Тарифные планы | ✅ | лимиты товаров/сотрудников, мягкое ограничение |
| FR-S04 | Глобальная аналитика | ✅ | GMV/MAU/состав магазинов/тренд |
| FR-S05 | Биллинг подписок | 🚫 | Stripe Billing — каркас вебхуков есть, логика отложена |

## Перечень оставшихся задач

### A. Заблокировано внешними решениями/ключами заказчика

1. **Stripe** — живой приём оплат (PaymentSheet/Google Pay, B05), сохранённые карты
   (B11), живой Refund (A11), Connect-онбординг + `onboard-link`,
   **биллинг подписок S05** (`/webhooks/stripe`, `/webhooks/stripe-billing`,
   dunning/grace), коллекции `invoices`/`subscription`. Каркас и env-gate готовы.
2. **Algolia** — полнотекстовый/фасетный поиск (B02) + `POST /api/search/reindex`
   (отсутствует). Сейчас работает серверный fallback-поиск.
3. **SMTP/Nodemailer** — email-подтверждение заказа (B05), уведомление владельцу
   при блокировке (S02).
4. **Resize extension** — деплой `firebase deploy --only extensions` для миниатюр
   (A02); конфиг в репозитории присутствует.
5. **ANTHROPIC_API_KEY** — живой AI-ассист (A12).
6. **SHA-отпечатки + домен** — живой Google Sign-In и App Links
   (`https://app.example.com/s/{slug}`).
7. **Устройство** — Baseline Profiles (производительность, §11.8/§12).

### B. Можно выполнять сейчас (не заблокировано)

Закрыто в ходе доработки (раздел B):

- ✅ **FR-A12** — режим **rewrite** описаний + Android-UI (PR #62). SEO-мета — Post-MVP.
- ✅ **FR-B11** — UI выбора адреса из книги уже был реализован (`AssistChip` в чекауте);
  остаются только сохранённые карты Stripe (раздел A).
- ✅ **Центр уведомлений (§11.5)** — in-app история push, бейдж непрочитанных (PR #63).
- ✅ **Force-update через Remote Config (§11.5)** — min-version gate, fail-open (PR #64).
  Этим **§11.5 закрыт полностью**.

Остаётся:

1. **Offline-first хвост:** каталог-офлайн (Paging 3 RemoteMediator), WorkManager
   (фоновая синхронизация при закрытом приложении), outbox для возвратов/остатка,
   обработка протухшего токена офлайн. (Ядро offline-first уже в `main`; по решению
   заказчика основной offline-трек принят готовым.)
2. **Тесты по §14:** E2E (Maestro) `регистрация→магазин→заказ`, load-тесты (k6)
   чекаут/поиск, E2E admin-web; perf-профайлинг.
3. **Модель данных §8:** при строгой сверке — вынести `stockNotifications` в
   коллекцию (сейчас массив в `customers/{uid}`; функционально работает).

### C. Post-MVP (зарезервировано самим ТЗ)

FR-A08 блог · FR-A10 лояльность · ML-рекомендации (Vertex AI / Recombee) ·
визуальный поиск · passkeys · мульти-регион Firestore (nam5).

## Реализованная инфраструктура и нефункциональные требования

- **Безопасность (§13):** App Check (env-gated), Firebase ID Token + Custom Claims,
  Firestore/Storage Rules (45+ тестов), rate-limit (Memorystore-путь задокументирован),
  двойная валидация (Kotlin + Zod), маскирование email в логах, auditLog + cron-очистка.
- **Тестирование (§14):** unit (ViewModel/Repository/Zod), integration (эмулятор
  Firestore), Rules-тесты, Android instrumentation, admin-web lint+build в CI.
  Пробелы: E2E (Maestro), load (k6), admin-web E2E.
- **Инфраструктура (§15):** Cloud Run, эмулятор-сьют, индексы, конфиг Resize
  extension, CI/CD (path-фильтры по компонентам), Remote Config (force-update).
  Пробел: Baseline Profiles (нужно устройство).
- **UI/UX (§11):** Material 3, edge-to-edge, предиктивный «назад», WindowSizeClass,
  локализация ru/en, a11y-подписи, **центр уведомлений** и **force-update** (§11.5
  закрыт полностью).
- **Offline-first:** HTTP-кэш + офлайн-фолбэк + баннер сети; Room-кэш «моих заказов»,
  заказов магазина и карточки товара; outbox смены статуса заказа + индикатор
  синхронизации; серверная идемпотентность статусов/возвратов/остатка.

## Хронология доработки (раздел B)

PR #61 (этот документ) · #62 FR-A12 rewrite · #63 центр уведомлений · #64 force-update.
Итог: §11.5 закрыт; FR-A12 доведён до MVP-полноты; FR-B11 подтверждён готовым
(кроме Stripe-карт). Остаток — раздел A (внешние ключи) и B-пункты 1–3 (offline-хвост
по согласованию принят готовым; тесты §14; строгая сверка модели §8).

---

Документ живой: обновляется по мере закрытия задач из разделов A/B.
