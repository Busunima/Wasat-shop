package com.wasat.shop.core.designsystem

import androidx.compose.material3.Typography

/**
 * Тип-шкала M3 (Display / Headline / Title / Body / Label) — ТЗ §11.2.
 * Используем дефолтную шкалу Material 3 на старте (одно семейство шрифтов);
 * кастомные ступени и шрифт магазина добавляются дизайн-системой в Фазе 1.
 */
internal val WasatTypography = Typography()
